# 🖥️ @dxo/studio-app

Local VMZ workbench for DXO training runs. Started by `dxo studio` (loopback).

Reads the append-only inspect store via the Studio HTTP API (`http://127.0.0.1:4310` by default).
