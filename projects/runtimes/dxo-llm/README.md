# @dxo/llm

**Workspace stub — not on npm publish / placeholder OIDC lists.**

Tokenizer and language-model product surface. Inference serving remains `@dxo/serve` (later). Prefer this package name over a vague `@dxo/text` umbrella for LLM-facing APIs.
