# @dxo/ui

**Placeholder — developer preview, API unstable.**

TypeScript model-app builders (`defineModelApp`). Long tasks use `AsyncIterable` + `AbortSignal` in the target contract. This stub does not serve HTTP yet; Studio Playground and independent VMZ deploy arrive in U3.
