# @dxo/studio

**Placeholder — developer preview, API unstable.**

Future VMZ workbench for runs, system metrics, artifacts, model graphs, profiler, and UI playground. This package currently only wires protocol package versions; it does **not** listen on a port yet.

Default future bind: loopback only. Remote deploy requires explicit auth (see Living design).
