# 🧭 @dxo/hub

**Workspace stub — not on npm publish / placeholder OIDC lists.**

Unified artifact providers (Hugging Face, ModelScope, local, HTTP, S3/R2). Not a UI. Dataset **artifacts** only — iteration stays in `@dxo/data`. Secrets stay on Node/server capability.
