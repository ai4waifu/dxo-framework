/** Lightweight CPU-only surface for browsers (M0 stub; WASM backend later). */
export function version(): string {
    return '0.1.0-dxo-lite';
}

/** Placeholder tensor (M0). */
export class Tensor {}
