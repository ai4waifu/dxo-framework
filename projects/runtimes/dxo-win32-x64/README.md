# 🪟 @dxo/dxo-win32-x64

Native Windows x64 runtime for DXO. `@dxo/core` selects this package automatically on a compatible Windows machine.

It contains the platform binary used by the public DXO API. Applications should import `@dxo/core`; direct imports from this package are unnecessary.
