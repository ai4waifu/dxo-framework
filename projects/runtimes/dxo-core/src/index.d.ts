/** Return the DXO engine version string from the native addon. */
export function version(): string;

/** Placeholder tensor handle (M0 scaffold). */
export declare class Tensor {
    constructor();
}
